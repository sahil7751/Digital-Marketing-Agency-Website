import { createMetadata } from "@/lib/site";
export const metadata = createMetadata({ title: "Careers", description: "Build meaningful growth work with the Lumora team.", path: "/career" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
