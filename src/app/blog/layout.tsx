import { JsonLd } from "@/components/seo/json-ld"; import { absoluteUrl, createMetadata, siteConfig } from "@/lib/site";
export const metadata = createMetadata({ title: "Journal", description: "Practical growth, creative, and marketing insights from Lumora.", path: "/blog" });
export default function Layout({ children }: { children: React.ReactNode }) { const blogPosting = { "@context": "https://schema.org", "@type": "BlogPosting", headline: "The growth metrics that matter when attention is expensive", description: "A clearer way to distinguish useful movement from activity that only looks busy.", author: { "@type": "Organization", name: siteConfig.name }, publisher: { "@type": "Organization", name: siteConfig.name }, mainEntityOfPage: absoluteUrl("/blog") }; return <><JsonLd data={blogPosting} />{children}</>; }
