import { createMetadata } from "@/lib/site";
export const metadata = createMetadata({ title: "Portfolio", description: "Explore selected growth, brand, and performance marketing work by Lumora.", path: "/portfolio" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
