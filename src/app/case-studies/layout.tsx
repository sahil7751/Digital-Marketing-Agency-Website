import { createMetadata } from "@/lib/site";
export const metadata = createMetadata({ title: "Case Studies", description: "In-depth client growth stories and marketing outcomes from Lumora.", path: "/case-studies" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
