import type { Metadata } from "next"; import { Check } from "lucide-react"; import { Breadcrumb } from "@/components/pages/breadcrumb"; import { PageCta } from "@/components/pages/page-cta"; import { PageHero } from "@/components/pages/page-hero"; import { Button } from "@/components/ui/button"; import { Container } from "@/components/ui/container"; import { Heading } from "@/components/ui/heading"; import { Section } from "@/components/ui/section";
export const metadata: Metadata = { title: "Pricing", description: "Flexible digital marketing engagements tailored to your business goals." };
const plans = [{name:"Growth audit",price:"From $6,000",text:"A focused engagement to find the clearest route to growth.",items:["Market and channel review","Customer journey analysis","90-day action plan"]},{name:"Growth partnership",price:"From $12,000/mo",text:"A cross-functional team that drives progress month after month.",items:["Strategy and measurement","Creative and media execution","Senior growth leadership"],featured:true},{name:"Embedded team",price:"Custom",text:"A bespoke growth unit designed around your internal capability.",items:["Dedicated specialists","Integrated planning","Executive reporting"]}];
export default function PricingPage() { return <><Breadcrumb current="Pricing" /><PageHero eyebrow="ENGAGEMENTS" title="Invest in progress, not activity." description="Our work is built around the challenge in front of you. These starting points make it easier to find the right conversation." cta="Find your fit" /><Section><Container><div className="grid gap-5 lg:grid-cols-3">{plans.map(plan => <article key={plan.name} className={`rounded-3xl border p-7 ${plan.featured ? "bg-slate-950 text-white" : "bg-surface"}`}><p className={`text-sm font-semibold ${plan.featured ? "text-cyan-300" : "text-primary"}`}>{plan.featured ? "MOST POPULAR" : "FLEXIBLE SCOPE"}</p><Heading as="h2" size="card" className={`mt-3 ${plan.featured ? "text-white" : ""}`}>{plan.name}</Heading><p className="mt-5 text-2xl font-semibold">{plan.price}</p><p className={`mt-3 min-h-12 text-sm leading-6 ${plan.featured ? "text-slate-300" : "text-muted-foreground"}`}>{plan.text}</p><ul className="my-8 space-y-3 text-sm">{plan.items.map(item => <li key={item} className="flex gap-2"><Check className={plan.featured ? "text-cyan-300" : "text-primary"} size={18} />{item}</li>)}</ul><Button href="/contact" variant={plan.featured ? "secondary" : "outline"} className="w-full">Talk to our team</Button></article>)}</div></Container></Section><PageCta title="Need a tailored team or scope?" text="We’ll shape an engagement around the outcome you are working towards." /></>; }
