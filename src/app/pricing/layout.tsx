import { createMetadata } from "@/lib/site";
export const metadata = createMetadata({ title: "Pricing", description: "Flexible digital marketing engagements tailored to your business goals.", path: "/pricing" });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
