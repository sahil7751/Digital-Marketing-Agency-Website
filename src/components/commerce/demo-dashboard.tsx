"use client";

import { BarChart3, IndianRupee, MousePointerClick, ShoppingBag, Users } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { getCommerceMetrics, recordDemoVisit, type CommerceMetrics } from "@/lib/commerce/storage";
import { plans } from "@/lib/commerce/plans";
import { Card } from "@/components/ui/card";

export function DemoDashboard() { const [metrics, setMetrics] = useState<CommerceMetrics | null>(null); useEffect(() => { recordDemoVisit(); setMetrics(getCommerceMetrics()); }, []); const popularPlan = useMemo(() => { if (!metrics?.purchases.length) return "No purchases yet"; const counts = metrics.purchases.reduce<Record<string, number>>((acc, purchase) => ({ ...acc, [purchase.planName]: (acc[purchase.planName] ?? 0) + 1 }), {}); return Object.entries(counts).sort((a, b) => b[1] - a[1])[0][0]; }, [metrics]); if (!metrics) return <div className="grid min-h-64 place-items-center text-muted-foreground">Loading dashboard…</div>; const conversionRate = metrics.visitors ? ((metrics.conversions / metrics.visitors) * 100).toFixed(1) : "0.0"; const maxCount = Math.max(1, ...plans.map((plan) => metrics.purchases.filter((purchase) => purchase.planId === plan.id).length)); return <div className="space-y-8"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={Users} label="Total visitors" value={metrics.visitors.toLocaleString("en-IN")} /><Metric icon={ShoppingBag} label="Conversions" value={String(metrics.conversions)} /><Metric icon={MousePointerClick} label="Conversion rate" value={`${conversionRate}%`} /><Metric icon={IndianRupee} label="Total revenue" value={`₹${metrics.revenue.toLocaleString("en-IN")}`} /></div><div className="grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><Card><div className="flex items-center justify-between"><div><p className="text-sm font-semibold text-primary">PLAN PERFORMANCE</p><h2 className="mt-2 text-2xl font-semibold">Most purchased: {popularPlan}</h2></div><BarChart3 className="text-primary" /></div><div className="mt-8 space-y-5">{plans.map((plan) => { const count = metrics.purchases.filter((purchase) => purchase.planId === plan.id).length; return <div key={plan.id}><div className="mb-2 flex justify-between text-sm"><span>{plan.name}</span><span className="font-semibold">{count}</span></div><div className="h-2 overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-gradient-to-r from-primary to-secondary" style={{ width: `${(count / maxCount) * 100}%` }} /></div></div>; })}</div></Card><Card><p className="text-sm font-semibold text-primary">FUNNEL HEALTH</p><dl className="mt-6 space-y-5"><Row label="Abandoned checkouts" value={String(metrics.abandonedCheckouts)} /><Row label="Average order value" value={metrics.conversions ? `₹${Math.round(metrics.revenue / metrics.conversions).toLocaleString("en-IN")}` : "₹0"} /><Row label="Mock revenue tracked" value="Local browser data" /></dl></Card></div><Card><p className="text-sm font-semibold text-primary">RECENT PURCHASES</p><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[520px] text-left text-sm"><thead className="border-b text-muted-foreground"><tr><th className="pb-3 font-medium">Customer</th><th className="pb-3 font-medium">Plan</th><th className="pb-3 font-medium">Amount</th><th className="pb-3 font-medium">Order</th></tr></thead><tbody>{metrics.purchases.length ? metrics.purchases.slice(0, 6).map((purchase) => <tr key={purchase.id} className="border-b last:border-0"><td className="py-4"><p className="font-medium">{purchase.customer.name}</p><p className="text-xs text-muted-foreground">{purchase.customer.company}</p></td><td className="py-4">{purchase.planName}</td><td className="py-4">{purchase.amount ? `₹${purchase.amount.toLocaleString("en-IN")}` : "Custom"}</td><td className="py-4 font-mono text-xs">{purchase.id}</td></tr>) : <tr><td colSpan={4} className="py-10 text-center text-muted-foreground">No completed demo purchases yet.</td></tr>}</tbody></table></div></Card></div>; }
function Metric({ icon: Icon, label, value }: { icon: typeof Users; label: string; value: string }) { return <Card className="p-5"><Icon size={19} className="text-primary" /><p className="mt-5 text-2xl font-semibold">{value}</p><p className="mt-1 text-sm text-muted-foreground">{label}</p></Card>; }
function Row({ label, value }: { label: string; value: string }) { return <div className="flex items-center justify-between border-b pb-4 text-sm last:border-0"><dt className="text-muted-foreground">{label}</dt><dd className="font-semibold">{value}</dd></div>; }
