import { Megaphone, Palette, Search, Workflow } from "lucide-react"; import { Card } from "@/components/ui/card"; import { Container } from "@/components/ui/container"; import { Heading } from "@/components/ui/heading"; import { Section } from "@/components/ui/section"; import { Reveal } from "./reveal";
const services = [{ icon: Search, title: "Growth strategy", text: "Clear positioning and a measurable plan for the next 12 months." }, { icon: Megaphone, title: "Performance media", text: "Campaigns engineered to turn every marketing dollar into momentum." }, { icon: Palette, title: "Brand & creative", text: "Memorable visual systems and content people genuinely want to share." }, { icon: Workflow, title: "Lifecycle marketing", text: "Automations that move prospects from first click to loyal customer." }];
export function ServicesPreview() { return <Section id="services"><Container><div className="max-w-2xl"><p className="text-sm font-semibold text-primary">WHAT WE DO</p><Heading className="mt-3">The full growth engine, under one roof.</Heading></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{services.map((service, i) => <Reveal key={service.title} delay={i * .06}><Card className="h-full transition duration-300 hover:-translate-y-1"><service.icon className="text-primary" size={25} /><Heading as="h3" size="card" className="mt-8">{service.title}</Heading><p className="mt-3 text-sm leading-6 text-muted-foreground">{service.text}</p></Card></Reveal>)}</div></Container></Section>; }
