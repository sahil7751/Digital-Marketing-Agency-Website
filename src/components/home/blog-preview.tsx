import { ArrowRight } from "lucide-react"; import { Container } from "@/components/ui/container"; import { Heading } from "@/components/ui/heading"; import { Section } from "@/components/ui/section";
const posts = [["Strategy", "The growth metrics that matter when attention is expensive", "6 min read"], ["Creative", "Why your best-performing ad rarely looks like an ad", "4 min read"], ["Insights", "A practical guide to building a resilient acquisition mix", "8 min read"]];
export function BlogPreview() { return <Section className="bg-muted/55"><Container><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-sm font-semibold text-primary">FROM THE JOURNAL</p><Heading className="mt-3">Ideas worth acting on.</Heading></div><a href="#newsletter" className="text-sm font-semibold text-primary hover:underline">Read the journal</a></div><div className="mt-10 grid gap-5 lg:grid-cols-3">{posts.map(([category, title, time], index) => <article key={title} className="group rounded-3xl bg-surface p-6"><div className={`aspect-[16/9] rounded-2xl bg-gradient-to-br ${["from-blue-400 to-cyan-300", "from-violet-500 to-fuchsia-400", "from-slate-700 to-slate-400"][index]}`} /><p className="mt-6 text-xs font-semibold uppercase tracking-wider text-primary">{category}</p><Heading as="h3" size="card" className="mt-3">{title}</Heading><div className="mt-6 flex items-center justify-between text-sm text-muted-foreground"><span>{time}</span><ArrowRight className="text-foreground transition group-hover:translate-x-1" size={18} /></div></article>)}</div></Container></Section>; }
