import { Check } from "lucide-react"; import { Button } from "@/components/ui/button"; import { Container } from "@/components/ui/container"; import { Heading } from "@/components/ui/heading"; import { Section } from "@/components/ui/section";
const plans = [{ id: "foundation", name: "Foundation", text: "For brands ready to build a smarter marketing baseline.", items: ["Growth audit", "Channel strategy", "Monthly insights"] }, { id: "acceleration", name: "Acceleration", text: "For teams serious about finding and scaling their growth levers.", items: ["Everything in Foundation", "Paid media management", "Creative testing program"], featured: true }, { id: "partnership", name: "Partnership", text: "For ambitious companies that need a fully embedded growth team.", items: ["Cross-channel execution", "Lifecycle automation", "Executive growth planning"] }];
export function PricingPreview() { return <Section id="pricing"><Container><div className="mx-auto max-w-2xl text-center"><p className="text-sm font-semibold text-primary">FLEXIBLE ENGAGEMENTS</p><Heading className="mt-3">A plan that meets your ambition.</Heading><p className="mt-4 text-muted-foreground">Every engagement is designed around outcomes. Start with a focused project or build an ongoing partnership.</p></div><div className="mt-10 grid gap-5 lg:grid-cols-3">{plans.map(plan => <article key={plan.name} className={`rounded-3xl border p-7 ${plan.featured ? "bg-slate-950 text-white shadow-xl" : "bg-surface"}`}><p className={`text-sm font-semibold ${plan.featured ? "text-cyan-300" : "text-primary"}`}>{plan.featured ? "MOST POPULAR" : ""}</p><Heading as="h3" size="card" className={`mt-2 ${plan.featured ? "text-white" : ""}`}>{plan.name}</Heading><p className={`mt-3 min-h-12 text-sm leading-6 ${plan.featured ? "text-slate-300" : "text-muted-foreground"}`}>{plan.text}</p><ul className="my-7 space-y-3 text-sm">{plan.items.map(item => <li key={item} className="flex gap-2"><Check className={plan.featured ? "text-cyan-300" : "text-primary"} size={18} />{item}</li>)}</ul><Button href={`/checkout?plan=${plan.id}`} variant={plan.featured ? "secondary" : "outline"} className="w-full">Choose Plan</Button></article>)}</div></Container></Section>; }
