import { Quote, Star } from "lucide-react"; import { Card } from "@/components/ui/card"; import { Container } from "@/components/ui/container"; import { Heading } from "@/components/ui/heading"; import { Section } from "@/components/ui/section";
const testimonials = [{ quote: "Lumora gave us the confidence to invest in growth again. The team connected our brand story to real pipeline, and results followed quickly.", name: "Amelia Ross", role: "CMO, Northline" }, { quote: "They operate like an extension of our leadership team: clear thinking, radical ownership, and creative that moves the needle.", name: "Daniel Kim", role: "Founder, Haven House" }];
export function Testimonials() { return <Section className="bg-muted/55"><Container><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-sm font-semibold text-primary">CLIENT STORIES</p><Heading className="mt-3">The work is better when trust is mutual.</Heading></div><div className="flex text-amber-400">{Array.from({ length: 5 }, (_, i) => <Star key={i} size={18} fill="currentColor" />)}</div></div><div className="mt-10 grid gap-5 lg:grid-cols-2">{testimonials.map(({ quote, name, role }) => <Card key={name}><Quote className="text-secondary" size={29} /><blockquote className="mt-7 text-xl font-medium leading-8 sm:text-2xl">“{quote}”</blockquote><div className="mt-8"><p className="font-semibold">{name}</p><p className="mt-1 text-sm text-muted-foreground">{role}</p></div></Card>)}</div></Container></Section>; }
